name: Android σåàµá╕µ₧äσ╗║-Φç¬σ«ÜΣ╣ë

# ΘÇéΘàì Node.js 24
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:
    inputs:
      # ==================== σåàµá╕τëêµ£¼Θàìτ╜« ====================
      android_version:
        description: "Android τëêµ£¼"
        required: true
        type: choice
        options:
          - android12
          - android13
          - android14
          - android15
          - android16
        default: android12
      kernel_version:
        description: "σåàµá╕τëêµ£¼"
        required: true
        type: choice
        options:
          - "5.10"
          - "5.15"
          - "6.1"
          - "6.6"
          - "6.12"
        default: "5.10"
      sub_level:
        description: "σ¡Éτëêµ£¼σÅ╖ (σªé 66, 198, X)"
        required: true
        type: string
        default: "66"
      os_patch_level:
        description: "σ«ëσà¿ΦíÑΣ╕üτ║ºσê½ (σªé 2022-01, lts)"
        required: true
        type: string
        default: "2022-01"
      revision:
        description: "Σ┐«Φ«óτëêµ£¼ (σªé r11, Σ╗à 5.10 Θ£ÇΦªü)"
        required: false
        type: string
        default: "r11"

      # ==================== KernelSU Θàìτ╜« ====================
      # KernelSU σÅÿΣ╜ôσ¢║σ«ÜΣ╕║ ReSukiSU
      kernelsu_branch:
        description: "KSU σêåµö» (main/builtin/Φç¬∩┐╜∩┐╜Σ╣ëcommit hash∩╝îτòÖτ⌐║Σ╜┐τö¿Θ╗ÿΦ«ñ)"
        required: false
        type: string
        default: ""

      # ==================== σÅ»ΘÇëΘàìτ╜« ====================
      version:
        description: "Φç¬σ«ÜΣ╣ëτëêµ£¼σÉì (σÅ»ΘÇë)"
        required: false
        type: string
      build_time:
        description: "Φç¬σ«ÜΣ╣ëµ₧äσ╗║µù╢Θù┤ (σÅ»ΘÇë∩╝îNµêûτòÖτ⌐║=σ╜ôσëìUTC)"
        required: false
        type: string
        default: "Sun Dec 01 08:10:00 UTC 2024"

      # ==================== σèƒΦâ╜σ╝Çσà│ ====================
      use_zram:
        description: "σÉ»τö¿ ZRAM σó₧σ╝║τ«ùµ│ò"
        required: true
        type: boolean
        default: false
      use_bbg:
        description: "σÉ»τö¿ BBG Θÿ▓µá╝µ£║"
        required: true
        type: boolean
        default: false
      use_kpm:
        description: "σÉ»τö¿ KPM σèƒΦâ╜"
        required: true
        type: boolean
        default: false
      use_rekernel:
        description: "σÉ»τö¿ Re-Kernel Θ⌐▒σè¿"
        required: false
        type: boolean
        default: false
      cancel_susfs:
        description: "τªüτö¿ SUSFS"
        required: true
        type: boolean
        default: false
      supp_op:
        description: "σÉ»τö¿Σ╕Çσèá 8E µö»µîü"
        required: false
        type: boolean
        default: false

jobs:
  build-custom-kernel:
    name: "${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.android_version }}-${{ inputs.os_patch_level }}"
    uses: ./.github/workflows/build.yml
    secrets: inherit
    with:
      android_version: ${{ inputs.android_version }}
      kernel_version: ${{ inputs.kernel_version }}
      sub_level: ${{ inputs.sub_level }}
      os_patch_level: ${{ inputs.os_patch_level }}
      revision: ${{ inputs.revision || '' }}
      ksu_variant: "ReSukiSU"
      ksu_branch: ${{ inputs.kernelsu_branch }}
      version: ${{ inputs.version || '' }}
      build_time: ${{ inputs.build_time || '' }}
      use_zram: ${{ inputs.use_zram }}
      use_bbg: ${{ inputs.use_bbg }}
      use_kpm: ${{ inputs.use_kpm }}
      use_rekernel: ${{ inputs.use_rekernel || false }}
      supp_op: ${{ inputs.supp_op || false }}
      enable_susfs: ${{ !inputs.cancel_susfs }}

  get-ksu-manager:
    uses: ./.github/workflows/get-manager.yml
    secrets: inherit
