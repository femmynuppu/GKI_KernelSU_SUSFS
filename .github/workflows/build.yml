# ========================================
# GKI såàµá+µPäs+¦s+ÑS+£µ¦ü
# ========================================
# µ¡ñs+ÑS+£µ¦üF¦ƒF¦út+ûF»æ Android GKI såàµá+n+îµö»µîüsñÜtºì KernelSU sÅÿS+ôsÆî SUSFS
# TÇÜF+ç workflow_call Fó½sà¦S+ûs+ÑS+£µ¦üF¦âtö¿n+îS+ìt¢¦µÄÑµëïsè¿FºªsÅæ
# ========================================

name: såàµá+µPäs+¦µ¦üt¿ï

# TÇéTàì Node.js 24
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  actions: write

on:
  workflow_call:
    inputs:
      # ==================== sƒ¦tíÇTàìt+« ====================
      android_version:
        required: true
        type: string
      kernel_version:
        required: true
        type: string
      sub_level:
        required: true
        type: string
      os_patch_level:
        required: true
        type: string

      # ==================== KernelSU Tàìt+« ====================
      ksu_variant:
        required: true
        type: string
      ksu_branch:
        required: true
        type: string

      # ==================== sÅ»TÇëTàìt+« ====================
      version:
        required: false
        type: string
      revision:
        required: false
        type: string
      build_time:
        required: false
        type: string
        default: "Sun Dec 01 08:10:00 UTC 2024"

      # ==================== sèƒFâ+s+Çsà¦ ====================
      use_zram:
        required: true
        type: boolean
        default: true
      use_bbg:
        required: true
        type: boolean
        default: true
      use_kpm:
        required: true
        type: boolean
        default: true
      enable_susfs:
        required: true
        type: boolean
        default: true
      supp_op:
        required: false
        type: boolean
        default: false

jobs:
  build-kernel:
    name: "${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.android_version }}-${{ inputs.os_patch_level }}-ReSukiSU"
    runs-on: ubuntu-latest
    timeout-minutes: 60

    # ==================== tÄ»sóâsÅÿTçÅTàìt+« ====================
    # ccache Tàìt+«tö¿S¦ÄsèáTÇƒTçìsñìt+ûF»æ
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"

    steps:
      # ==================== µPäs+¦S+íµü»µæÿFªü ====================
      # µëôsì¦s+ôsëìµPäs+¦tÜäµëÇµ£ëTàìt+«S+íµü»n+îµû¦S++F¦âF»òsÆîF++F+¬
      - name: µPäs+¦S+íµü»µæÿFªü
        run: |
          echo "========================================"
          echo "       såàµá+µPäs+¦Tàìt+«µæÿFªü"
          echo "========================================"
          echo "Android tëêµ£¼  : ${{ inputs.android_version }}"
          echo "såàµá+tëêµ£¼      : ${{ inputs.kernel_version }}"
          echo "s¡Étëêµ£¼sÅ+      : ${{ inputs.sub_level }}"
          echo "FíÑS+üt¦ºsê½      : ${{ inputs.os_patch_level }}"
          echo "KSU sÅÿS+ô      : ReSukiSU"
          echo "KSU sêåµö»      : ${{ inputs.ksu_branch }}"
          echo "µPäs+¦µù¦Tù¦      : ${{ inputs.build_time }}"
          echo "SUSFS tè¦µÇü    : ${{ inputs.enable_susfs }}"
          echo "ZRAM sóPs+¦     : ${{ inputs.use_zram }}"
          echo "BBG FíÑS+ü      : ${{ inputs.use_bbg }}"
          echo "KPM sèƒFâ+      : ${{ inputs.use_kpm }}"
          echo "Stock Config  : Fç¬sè¿µúÇµ¦ï config/stock_defconfig"
          echo "========================================"

      # ==================== µ+àtÉåtúüt¢ÿt¬¦Tù¦ ====================
      # GitHub Actions T+ÿF«ñtúüt¢ÿt¬¦Tù¦µ£ëTÖÉn+îT£ÇFªüµ+àtÉåS+ìs+àFªütÜäTóäFúàF+»S+¦
      - name: µ+àtÉåtúüt¢ÿt¬¦Tù¦
        uses: endersonmenezes/free-disk-space@v3
        with:
          remove_android: true
          remove_dotnet: true
          remove_haskell: true
          remove_tool_cache: true
          remove_swap: true
          remove_packages: "azure-cli google-cloud-cli microsoft-edge-stable google-chrome-stable firefox postgresql* temurin-* *llvm* mysql* dotnet-sdk-*"
          remove_packages_one_command: true
          remove_folders: "/usr/share/swift /usr/share/miniconda /usr/share/az* /usr/local/lib/node_modules /usr/local/share/chromium /usr/local/share/powershell /usr/local/julia /usr/local/aws-cli /usr/local/aws-sam-cli /usr/share/gradle"
          rm_cmd: "rmz"
          rmz_version: "3.1.1"
          testing: false

      # ==================== µúÇsç¦S+ôs¦ôS+útáü ====================
      - name: µúÇsç¦S+útáüS+ôs¦ô
        uses: actions/checkout@v6

      # ==================== sê¥sºïsîûµPäs+¦tÄ»sóâ ====================
      # F«+t+«sÉätºìF+»s+äsÅÿTçÅn+îsîàµï¼såàµá+µ¦ÉtáüpÇüFíÑS+üpÇüs+Ñsà+t¡ët¢«s+ò
      - name: sê¥sºïsîûµPäs+¦tÄ»sóâ
        run: |
          # µPäs+¦Tàìt+«µáçF»å
          CONFIG="${{ inputs.android_version }}-${{ inputs.kernel_version }}-${{ inputs.sub_level }}"
          KERNEL_ROOT="$GITHUB_WORKSPACE/$CONFIG"
          mkdir -p "$KERNEL_ROOT"

          # s»+sç¦tÄ»sóâsÅÿTçÅsê¦sÉÄt+¡µ¡ÑT¬ñ
          cat >> $GITHUB_ENV << EOF
          CONFIG=$CONFIG
          KERNEL_ROOT=$KERNEL_ROOT
          DEFCONFIG=$KERNEL_ROOT/common/arch/arm64/configs/gki_defconfig
          SUSFS4KSU=$GITHUB_WORKSPACE/susfs4ksu
          KERNEL_PATCHES=$GITHUB_WORKSPACE/kernel_patches
          SUKISU_PATCHES=$GITHUB_WORKSPACE/SukiSU_patch
          ZZH_PATCHES=$GITHUB_WORKSPACE
          ANYKERNEL3=$GITHUB_WORKSPACE/AnyKernel3
          ACTION_BUILD=$GITHUB_WORKSPACE/Action-Build
          AVBTOOL=$GITHUB_WORKSPACE/kernel-build-tools/linux-x86/bin/avbtool
          MKBOOTIMG=$GITHUB_WORKSPACE/mkbootimg/mkbootimg.py
          UNPACK_BOOTIMG=$GITHUB_WORKSPACE/mkbootimg/unpack_bootimg.py
          BOOT_SIGN_KEY_PATH=$GITHUB_WORKSPACE/kernel-build-tools/linux-x86/share/avb/testkey_rsa2048.pem
          EOF

          # S+ïF++s¦¦Tàìt+« repo s+Ñsà+ (tö¿S¦ÄsÉîµ¡Ñ Android såàµá+µ¦Étáü)
          mkdir -p "$GITHUB_WORKSPACE/git-repo"
          curl -L https://storage.googleapis.com/git-repo-downloads/repo -o "$GITHUB_WORKSPACE/git-repo/repo"
          chmod 0755 "$GITHUB_WORKSPACE/git-repo/repo"
          echo "$GITHUB_WORKSPACE/git-repo" >> "$GITHUB_PATH"
          echo "REPO=$GITHUB_WORKSPACE/git-repo/repo" >> $GITHUB_ENV

      # ==================== µÿ+tñ¦Tàìt+«µûçS+¦såàs«¦ ====================
      - name: µÿ+tñ¦Tàìt+«S+íµü»
        run: |
          CONFIG_FILE="$GITHUB_WORKSPACE/config/config"
          if [ -f "$CONFIG_FILE" ]; then
            echo "sèáF++Tàìt+«µûçS+¦: $CONFIG_FILE"
            cat "$CONFIG_FILE"
          fi

      # ==================== s«ëFúàt+ûF»æS+¥F¦û ====================
      # s«ëFúàsåàµá+t+ûF»æµëÇT£ÇtÜäsÉätºìs+Ñsà+sÆîs¦ôn+êS++tö¿ apt t+ôs¡ÿsèáTÇƒn+ë
      - name: s«ëFúàt+ûF»æS+¥F¦û
        uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: ccache python3 git curl build-essential libssl-dev bison flex libelf-dev dwarves
          execute_install_scripts: true

      # ==================== Tàìt+« ccache ====================
      # ccache tö¿S¦Ät+ôs¡ÿt+ûF»æt+ôµP£n+îsèáTÇƒsÉÄt+¡µPäs+¦
      - name: Tàìt+« ccache
        run: |
          mkdir -p ~/.cache/bazel
          ccache --version
          ccache --max-size=2G
          ccache --set-config=compression=true
          echo "CCACHE_DIR=$HOME/.ccache" >> $GITHUB_ENV

      # ==================== µüósñì ccache t+ôs¡ÿ ====================
      - name: µüósñì ccache t+ôs¡ÿ
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ${{ inputs.android_version }}-${{ inputs.kernel_version }}-${{ inputs.sub_level }}-ccache-${{ github.sha }}
          restore-keys: |
            ${{ inputs.android_version }}-${{ inputs.kernel_version }}-${{ inputs.sub_level }}-ccache-

      # ==================== t+ôs¡ÿs+Ñsà+Tô+ ====================
      # t+ôs¡ÿ AOSP µPäs+¦s+Ñsà+n+îTü+sàìµ»Åµ¼íTçìµû¦S+ïF++
      - name: t+ôs¡ÿs+Ñsà+Tô+
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: |
            kernel-build-tools
            mkbootimg
          key: toolchain-${{ runner.os }}-v1

      # ==================== S+ïF++s+Ñsà+Tô+ ====================
      # S+Ä AOSP Tò£sâÅS+ïF++såàµá+µPäs+¦s+Ñsà+sÆî mkbootimg
      - name: S+ïF++s+Ñsà+Tô+
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          AOSP_MIRROR=https://android.googlesource.com
          BRANCH=main-kernel-build-2024
          git clone $AOSP_MIRROR/kernel/prebuilts/build-tools -b $BRANCH --depth 1 kernel-build-tools
          git clone $AOSP_MIRROR/platform/system/tools/mkbootimg -b $BRANCH --depth 1 mkbootimg

      # ==================== töƒµêÉt¡+sÉìs»åTÆÑ ====================
      # töƒµêÉtö¿S¦Ä boot.img t¡+sÉìtÜä RSA s»åTÆÑ
      - name: töƒµêÉt¡+sÉìs»åTÆÑ
        run: |
          openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 > $BOOT_SIGN_KEY_PATH

      # ==================== Tàìt+« Git ====================
      - name: Tàìt+« Git
        run: |
          git config --global user.name "BuildBot"
          git config --global user.email "BuildGkiKernel@gmail.com"

      # ==================== sàïTÜåS+¥F¦ûS+ôs¦ô ====================
      # sàïTÜå AnyKernel3pÇüSUSFSpÇüsÉätºìFíÑS+üS+ôs¦ô
      - name: sàïTÜåS+¥F¦ûS+ôs¦ô
        run: |
          ANYKERNEL_BRANCH="gki-2.0"
          SUSFS_BRANCH="gki-${{ inputs.android_version }}-${{ inputs.kernel_version }}"

          echo "sàïTÜå AnyKernel3..."
          git clone https://github.com/WildKernels/AnyKernel3.git -b "$ANYKERNEL_BRANCH"
          rm -rf AnyKernel3/.git

          echo "sàïTÜå SUSFS (sêåµö»: $SUSFS_BRANCH)..."
          # ReSukiSU S++tö¿ simonpunk tÜäsÄƒtëê SUSFS
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b "$SUSFS_BRANCH"

          # µúÇµƒÑµÿ»sÉªsÉ»tö¿Fç¬s«ÜS¦ëµÅÉS¦ñ
          CONFIG_FILE="config/config"
          if [ -f "$CONFIG_FILE" ]; then
            CUSTOM_ENABLED=$(grep "^custom=" "$CONFIG_FILE" | cut -d'=' -f2)
            if [ "$CUSTOM_ENABLED" == "true" ]; then
              CUSTOM_COMMIT=$(grep "^${SUSFS_BRANCH}=" "$CONFIG_FILE" | cut -d'=' -f2)
              if [ -n "$CUSTOM_COMMIT" ]; then
                echo "sêçµìó SUSFS sê¦Fç¬s«ÜS¦ëµÅÉS¦ñ: $CUSTOM_COMMIT"
                cd susfs4ksu
                git checkout "$CUSTOM_COMMIT"
                cd ..
              fi
            fi
          fi

          SUSFS_LATEST_COMMIT_DATE=$(git -C susfs4ksu log -1 --date=format:'%Y-%m-%d %H:%M:%S %z' --format='%cd')
          echo "SUSFS_LATEST_COMMIT_DATE=$SUSFS_LATEST_COMMIT_DATE" >> $GITHUB_ENV
          echo "SUSFS S+ôs¦ôµ£Çµû¦µÅÉS¦ñµùÑµ£ƒ: $SUSFS_LATEST_COMMIT_DATE"

          echo "sçåsñçFíÑS+üF¦äµ¦É..."
          git clone https://github.com/WildKernels/kernel_patches.git
          git clone https://github.com/ShirkNeko/SukiSU_patch.git
          echo "S++tö¿s+ôsëìS+ôs¦ôFíÑS+üt¢«s+ò: $ZZH_PATCHES"
          git clone https://github.com/Numbersf/Action-Build.git --depth=1

      # ==================== sê¥sºïsîûs¦¦sÉîµ¡Ñsåàµá+µ¦Étáü ====================
      # S++tö¿ repo s+Ñsà+S+Ä AOSP FÄ+sÅû GKI såàµá+µ¦Étáü
      - name: sê¥sºïsîûs¦¦sÉîµ¡Ñsåàµá+µ¦Étáü
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          # sàïTÜå GCC s+Ñsà+Tô+ (tö¿S¦Ä 5.x såàµá+)
          git clone https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-gnu-6.4.1.git gcc

          FORMATTED_BRANCH="${{ inputs.android_version }}-${{ inputs.kernel_version }}-${{ inputs.os_patch_level }}"
          echo "sê¥sºïsîû repon+îsêåµö»: common-${FORMATTED_BRANCH}"

          $REPO init --depth=1 -u https://android.googlesource.com/kernel/manifest -b common-${FORMATTED_BRANCH} --repo-rev=v2.16

          # µúÇµƒÑµÿ»sÉªS+¦s+¦s+âtö¿tÜäsêåµö»
          REMOTE_BRANCH=$(git ls-remote https://android.googlesource.com/kernel/common ${FORMATTED_BRANCH})
          DEFAULT_MANIFEST_PATH=.repo/manifests/default.xml

          if grep -q deprecated <<< $REMOTE_BRANCH; then
            echo "µúÇµ¦ïsê¦s+¦s+âtö¿tÜäsêåµö»: $FORMATTED_BRANCH"
            sed -i "s/\"${FORMATTED_BRANCH}\"/\"deprecated\/${FORMATTED_BRANCH}\"/g" $DEFAULT_MANIFEST_PATH
          fi

          $REPO --trace sync -c -j$(nproc --all) --no-tags --fail-fast
          echo "REMOTE_BRANCH=$REMOTE_BRANCH" >> $GITHUB_ENV

      # ==================== S+¬Fúà /proc/config.gzn+êFç¬sè¿µúÇµ¦ïn+ë ====================
      # FïÑS+ôs¦ôs¡ÿs£¿ config/stock_defconfign+îsêÖFç¬sè¿s¦ötö¿n+¢S+ìs¡ÿs£¿sêÖF+¦F+ç
      - name: s¦ötö¿ Stock Config S+¬Fúà
        run: |
          # S+ôs¦ôsåàtÜä stock_defconfig µ¥Ñµ¦ÉS+Äsåàµá+µ¦Étáüt¢«µáçF+»s+ä
          STOCK_SRC="$GITHUB_WORKSPACE/config/stock_defconfig"
          STOCK_DST="$KERNEL_ROOT/common/arch/arm64/configs/stock_defconfig"

          # µ£¬µÅÉS+¢ stock_defconfig µù¦t¢¦µÄÑF+¦F+çn+îS+ìs+¦sôìS++µ¦üt¿ï
          if [ ! -f "$STOCK_SRC" ]; then
            echo "µ£¬µúÇµ¦ïsê¦ $STOCK_SRCn+îF+¦F+ç Stock Config S+¬FúàpÇé"
            exit 0
          fi

          # µÅÉsëìtí«S+¥t¢«µáçt¢«s+òs¡ÿs£¿n+îTü+sàì cp s¢át¢«s+òt+¦sñ¦sñ¦F¦Ñ
          mkdir -p "$(dirname "$STOCK_DST")"
          if ! cp "$STOCK_SRC" "$STOCK_DST"; then
            echo "::error::sñìsê¦ stock_defconfig sñ¦F¦Ñ: $STOCK_SRC -> $STOCK_DST"
            exit 1
          fi
          echo "s+¦sñìsê¦ stock_defconfig -> $STOCK_DST"

          # FºäsêÖµ¢+µìót¢«µáçn+Üs¢¦s«ÜS+¦ common/kernel/Makefilen+êS+Äs+ôsëìt¢«s+òt+ôµPäS+ÇFç¦n+ë
          NEW_RULE='$(obj)/config_data: arch/arm64/configs/stock_defconfig FORCE'
          OLD_RULE='$(obj)/config_data: $(KCONFIG_CONFIG) FORCE'
          TARGET_MAKEFILE="$KERNEL_ROOT/common/kernel/Makefile"

          # µ£¬µë+sê¦t¢«µáç Makefile t¢¦µÄÑsñ¦F¦Ñn+îTÿ¦µ¡óF»»µö¦sà¦s«âµûçS+¦
          if [ ! -f "$TARGET_MAKEFILE" ]; then
            echo "::error::µ£¬µë+sê¦ $TARGET_MAKEFILE"
            exit 1
          fi

          # S+àsàüF«+S+ñtºìtè¦µÇün+Üs+¦µÿ»µû¦FºäsêÖ / sæ+S+¡µùºFºäsêÖs¦¦µ¢+µìón+¢sà¦s«âµâàså¦S+Çs+ïµèÑTöÖ
          if grep -qF "$NEW_RULE" "$TARGET_MAKEFILE"; then
            echo "config_data FºäsêÖs+¦µÿ» stock_defconfign+îF+¦F+çpÇé"
          elif grep -qF "$OLD_RULE" "$TARGET_MAKEFILE"; then
            sed -i 's|\$(obj)/config_data: \$(KCONFIG_CONFIG) FORCE|$(obj)/config_data: arch/arm64/configs/stock_defconfig FORCE|' "$TARGET_MAKEFILE"
            echo "s+¦µ¢+µìó config_data FºäsêÖ: $TARGET_MAKEFILE"
          else
            echo "::error::µ£¬s£¿ $TARGET_MAKEFILE µë+sê¦FºäsêÖ: $OLD_RULE"
            exit 1
          fi

      # ==================== µÅÉsÅûs«PTÖàs¡Étëêµ£¼sÅ+ ====================
      # S+Ä Makefile S+¡F»+sÅût£ƒs«PtÜä SUBLEVEL sÇ+
      - name: µÅÉsÅûs«PTÖàs¡Étëêµ£¼sÅ+
        run: |
          ACTUAL_SUBLEVEL="${{ inputs.sub_level }}"
          if [[ -f "$KERNEL_ROOT/common/Makefile" ]]; then
            EXTRACTED=$(grep '^SUBLEVEL = ' "$KERNEL_ROOT/common/Makefile" | awk '{print $3}')
            [[ -n "$EXTRACTED" ]] && ACTUAL_SUBLEVEL="$EXTRACTED"
          fi
          echo "ACTUAL_SUBLEVEL=$ACTUAL_SUBLEVEL" >> $GITHUB_ENV
          echo "s«PTÖàs¡Étëêµ£¼sÅ+: $ACTUAL_SUBLEVEL"

      # ==================== S+«sñì glibc 2.38 sà+s«¦µÇºTù«Tóÿ ====================
      # µƒÉS¦¢µùºtëêsåàµá+s£¿µû¦tëê glibc S+èt+ûF»æS+Üsç¦TöÖn+îT£ÇFªüµëôFíÑS+ü
      - name: S+«sñì glibc 2.38 sà+s«¦µÇº
        working-directory: ${{ env.KERNEL_ROOT }}/common
        run: |
          RAW_SUB="${{ inputs.sub_level }}"
          [[ ! "$RAW_SUB" =~ ^[0-9]+$ ]] && CURRENT_SUB=99999 || CURRENT_SUB=$RAW_SUB

          NEEDS_FIX=false
          if [[ "${{ inputs.android_version }}" == "android13" && "${{ inputs.kernel_version }}" == "5.10" && $CURRENT_SUB -le 186 ]] ||
             [[ "${{ inputs.android_version }}" == "android13" && "${{ inputs.kernel_version }}" == "5.15" && $CURRENT_SUB -le 119 ]] ||
             [[ "${{ inputs.android_version }}" == "android14" && "${{ inputs.kernel_version }}" == "6.1" && $CURRENT_SUB -le 43 ]]; then
            NEEDS_FIX=true
          fi

          if [ "$NEEDS_FIX" = true ]; then
            GLIBC_VERSION=$(ldd --version 2>/dev/null | head -n 1 | awk '{print $NF}')
            if [ "$(printf '%s\n' "2.38" "$GLIBC_VERSION" | sort -V | head -n1)" = "2.38" ]; then
              echo "s¦ötö¿ glibc 2.38 sà+s«¦µÇºS+«sñì..."
              sed -i '/\$(Q)\$(MAKE) -C \$(SUBCMD_SRC) OUTPUT=\$(abspath \$(dir \$@))\/ \$(abspath \$@)/s//$(Q)$(MAKE) -C $(SUBCMD_SRC) EXTRA_CFLAGS="$(CFLAGS)" OUTPUT=$(abspath $(dir $@))\/ $(abspath $@)/' tools/bpf/resolve_btfids/Makefile 2>/dev/null || true

              if [[ "${{ inputs.kernel_version }}" == "5.10" || "${{ inputs.kernel_version }}" == "5.15" ]]; then
                sed -i '/char \*buf = NULL;/a int i;' tools/lib/subcmd/parse-options.c 2>/dev/null || true
                sed -i 's/for (int i = 0; subcommands\[i\]; i++) {/for (i = 0; subcommands[i]; i++) {/' tools/lib/subcmd/parse-options.c 2>/dev/null || true
                sed -i '/if (subcommands) {/a int i;' tools/lib/subcmd/parse-options.c 2>/dev/null || true
                sed -i 's/for (int i = 0; subcommands\[i\]; i++)/for (i = 0; subcommands[i]; i++)/' tools/lib/subcmd/parse-options.c 2>/dev/null || true
              fi
            fi
          fi

      # ==================== S+Çsèá 8E sñätÉåsÖ¿µö»µîü ====================
      # µ++sèáS+Çsèá 8E sñätÉåsÖ¿µ£¦sPïtÜäµö»µîüFíÑS+ü
      - name: µ++sèáS+Çsèá 8E sñätÉåsÖ¿µö»µîü
        if: inputs.supp_op
        working-directory: ${{ env.KERNEL_ROOT }}/common/drivers
        run: |
          echo "S+ïF++S+Çsèá 8E µö»µîüFíÑS+ü..."
          curl -LSs "https://github.com/zzh20188/GKI_KernelSU_SUSFS/raw/refs/heads/dev/hmbird_patch.c" -o hmbird_patch.c
          echo "obj-y += hmbird_patch.o" >> Makefile

      # ==================== tí«s«Ü KernelSU sêåµö» ====================
      # ReSukiSU µö»µîüFç¬s«ÜS¦ësêåµö»µêû commit hash
      - name: tí«s«Ü KernelSU sêåµö»
        id: ksu-branch
        run: |
          branch_input="${{ inputs.ksu_branch }}"

          # sªéµP£tö¿µê+F+ôsàÑS¦åFç¬s«ÜS¦ësêåµö»sÉìµêû commit hashn+ît¢¦µÄÑS++tö¿
          if [ -n "$branch_input" ]; then
            BRANCH="-s $branch_input"
            echo "S++tö¿Fç¬s«ÜS¦ësêåµö»/µÅÉS¦ñ: $branch_input"
          else
            # ReSukiSU T+ÿF«ñS++tö¿ main sêåµö»
            BRANCH="-s main"
          fi

          echo "BRANCH=$BRANCH" >> $GITHUB_ENV
          echo "KSU sêåµö»: $BRANCH"

      # ==================== µ++sèá KernelSU ====================
      # µ++sèá ReSukiSU
      - name: µ++sèá KernelSU
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          echo "µ++sèá ReSukiSU..."
          curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/main/kernel/setup.sh" | bash $BRANCH

          if [ -d "KernelSU/.git" ]; then
            KSU_LATEST_COMMIT_DATE=$(git -C KernelSU log -1 --date=format:'%Y-%m-%d %H:%M:%S %z' --format='%cd')
            echo "KSU_LATEST_COMMIT_DATE=$KSU_LATEST_COMMIT_DATE" >> $GITHUB_ENV
            echo "KSU S+ôs¦ôµ£Çµû¦µÅÉS¦ñµùÑµ£ƒ: $KSU_LATEST_COMMIT_DATE"
          else
            echo "KSU_LATEST_COMMIT_DATE=µ£¬tƒÑ" >> $GITHUB_ENV
          fi

      # ==================== s¦ötö¿ SUSFS FíÑS+ü ====================
      # SUSFS (Super User SUS FileSystem) tö¿S¦ÄTÜÉFùÅ root tùòF+¦
      - name: s¦ötö¿ SUSFS FíÑS+ü
        if: inputs.enable_susfs
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          echo "s¦ötö¿ SUSFS FíÑS+ü..."
          SUSFS_VERSION="2.0.0"

          # sñìsê¦ SUSFS sƒ¦tíÇµûçS+¦
          cp $SUSFS4KSU/kernel_patches/50_add_susfs_in_gki-${{ inputs.android_version }}-${{ inputs.kernel_version }}.patch ./common/
          cp $SUSFS4KSU/kernel_patches/fs/* ./common/fs/
          cp $SUSFS4KSU/kernel_patches/include/linux/* ./common/include/linux/

          case "${{ inputs.ksu_variant }}" in
            "ReSukiSU")
              echo "ReSukiSU S++tö¿såàt+« SUSFS µö»µîü"
              ;;
          esac

          # s¦ötö¿S++ SUSFS FíÑS+üsê¦såàµá+
          cd $KERNEL_ROOT/common
          patch -p1 < 50_add_susfs_in_gki-${{ inputs.android_version }}-${{ inputs.kernel_version }}.patch || true
          
          # FIX: vm_flags_clear & vm_flags_set tidak ada di Kernel 5.10 / 5.15
          # Ganti sintaks wrapper 6.1+ menjadi sintaks bitwise manual 5.10
          if [[ "${{ inputs.kernel_version }}" == "5.10" || "${{ inputs.kernel_version }}" == "5.15" ]]; then
            sed -i 's/vm_flags_clear(new_vma, VM_PAD_MASK);/new_vma->vm_flags \&= ~VM_PAD_MASK;/g' ./mm/mmap.c || true
            sed -i 's/vm_flags_set(new_vma, VM_PAD_MASK);/new_vma->vm_flags |= VM_PAD_MASK;/g' ./mm/mmap.c || true
          fi

          # s¦ötö¿tëêµ£¼të¦s«ÜS+«sñì
          if [ "${{ inputs.ksu_variant }}" == "ReSukiSU" ]; then
            CURRENT_SUB="${{ inputs.sub_level }}"
            if [[ ! "$CURRENT_SUB" =~ ^[0-9]+$ ]]; then
              CURRENT_SUB=99999
            fi

            # Android 12 - 5.10 S+«sñì
            if [[ "${{ inputs.android_version }}" == "android12" && "${{ inputs.kernel_version }}" == "5.10" ]]; then
              if [[ "$CURRENT_SUB" -le 209 ]]; then
                sed -i -e 's/goto show_pad;/return 0;/' ./fs/proc/task_mmu.c
              fi
              if [[ "$CURRENT_SUB" -le 117 ]]; then
                cp $KERNEL_PATCHES/wild/susfs_fix_patches/v${SUSFS_VERSION}/a12-5.10/fdinfo.c.patch ./
                patch -p1 < fdinfo.c.patch || true
                # S+«sñì 5.10.x Gëñ 117 såàµá+t+¦s¦æ inotify_mark_user_mask sç+µò¦tÜät+ûF»æTöÖF»»
                # SUSFS v2.0.0 s£¿ fdinfo.c S+¡s+òsàÑS¦åµ¡ñsç+µò¦, S+åF»Ñsç+µò¦s£¿ 5.10.117 sÅèS+ÑS+ïsåàµá+tëêµ£¼S+¡S+ìs¡ÿs£¿
                # S++tö¿t¡ëµòêFí¿F++s+Å mark->mask & IN_ALL_EVENTS µ¢+S+ú
                sed -i 's/inotify_mark_user_mask(mark)/mark->mask \& IN_ALL_EVENTS/g' ./fs/notify/fdinfo.c
              fi
              if [[ "$CURRENT_SUB" -le 43 ]]; then
                cp $KERNEL_PATCHES/wild/susfs_fix_patches/v${SUSFS_VERSION}/a12-5.10/base.c.patch ./
                patch -p1 < base.c.patch || true
                # TODO: t¡ës+à WildKernels µ¢¦µû¦FíÑS+üsÉÄtº+TÖñµ¦¿Tçè
                # # S+«sñì WildKernels FíÑS+üS+¡tÜäµùºt¼ªsÅ+ BIT_SUS_MAPS (SUSFS S+ôs¦ôs+¦Tçìsæ+sÉìS+¦ AS_FLAGS_SUS_MAP + test_bit)
                # if grep -q 'BIT_SUS_MAPS' ./fs/proc/base.c; then
                #   sed -i 's|unlikely(inode->i_mapping->flags & BIT_SUS_MAPS) && susfs_is_current_proc_umounted()|inode->i_mapping \&\& unlikely(test_bit(AS_FLAGS_SUS_MAP, \&inode->i_mapping->flags) \&\& susfs_is_current_proc_umounted_app())|g' ./fs/proc/base.c
                # fi
              fi
              # TODO: t¡ës+à WildKernels µ¢¦µû¦FíÑS+üsÉÄtº+TÖñµ¦¿Tçè
              # # tí«S+¥ base.c sîàsÉ½ susfs_def.h (SUSFS S++FíÑS+üsƒ¦S¦Ä 5.10.246 Tçìs+¦, include hunk sÅ»Fâ+T¥ÖT+ÿsñ¦F¦Ñ)
              # if grep -q 'AS_FLAGS_SUS_MAP\|susfs_is_current_proc_umounted' ./fs/proc/base.c && ! grep -qF 'susfs_def.h' ./fs/proc/base.c; then
              #   sed -i '/#include <linux\/cpufreq_times.h>/a #endif' ./fs/proc/base.c
              #   sed -i '/#include <linux\/cpufreq_times.h>/a #include <linux\/susfs_def.h>' ./fs/proc/base.c
              #   sed -i '/#include <linux\/cpufreq_times.h>/a #ifdef CONFIG_KSU_SUSFS_SUS_MAP' ./fs/proc/base.c
              # fi
            fi
          

            # Android 13 - 5.15 S+«sñì
            if [[ "${{ inputs.android_version }}" == "android13" && "${{ inputs.kernel_version }}" == "5.15" ]]; then
              if [[ "$CURRENT_SUB" -le 41 ]]; then
                cp $KERNEL_PATCHES/wild/susfs_fix_patches/v${SUSFS_VERSION}/a13-5.15/namespace.c.patch ./
                patch -p1 < namespace.c.patch || true
                cp $KERNEL_PATCHES/wild/susfs_fix_patches/v${SUSFS_VERSION}/a13-5.15/fdinfo.c.patch ./
                patch -p1 < fdinfo.c.patch || true
                cp $KERNEL_PATCHES/wild/susfs_fix_patches/v${SUSFS_VERSION}/a13-5.15/open.c.patch ./
                patch -p1 < open.c.patch || true
                # TODO: t¡ës+à WildKernels µ¢¦µû¦FíÑS+üsÉÄtº+TÖñµ¦¿Tçè
                # # S+«sñì i_user_ns: 5.15.41 µ¦íµ£ëµ¡ñsç+µò¦, SUSFS S+ôs¦ôTçìs+¦sÉÄµû¦sóPS¦å &fi->inode sÅÿS+ô
                # sed -i 's|is_i_uid_not_allowed(i_uid_into_mnt(i_user_ns(&fi->inode), &fi->inode).val)))|is_i_uid_not_allowed(fi->inode.i_uid.val)))|g' fs/susfs.c
                # sed -i 's|is_i_uid_not_allowed(i_uid_into_mnt(i_user_ns(inode), inode).val)))|is_i_uid_not_allowed(inode->i_uid.val)))|g' fs/susfs.c
              fi
              if [[ "$CURRENT_SUB" -le 148 && "${{ inputs.os_patch_level }}" != "2024-05" ]]; then
                sed -i -e 's/goto show_pad;/return 0;/' ./fs/proc/task_mmu.c
              fi
            fi


            # Android 14 - 6.1 S+«sñì
            if [[ "${{ inputs.android_version }}" == "android14" && "${{ inputs.kernel_version }}" == "6.1" ]]; then
              if [[ "$CURRENT_SUB" -le 141 || "$CURRENT_SUB" -ge 145 ]]; then
                cp $KERNEL_PATCHES/wild/susfs_fix_patches/v${SUSFS_VERSION}/a14-6.1/base.c.patch ./
                patch -p1 < base.c.patch || true
              fi
              if ! grep -qF '#include <linux/dma-buf.h>' ./fs/proc/base.c; then
                sed -i '/#include <linux\/cpufreq_times.h>/a #include <linux\/dma-buf.h>' ./fs/proc/base.c
              fi
              if grep -q 'susfs_is_current_proc_umounted' ./fs/proc/base.c && ! grep -qF '#include <linux/susfs.h>' ./fs/proc/base.c; then
                sed -i '/#include <linux\/dma-buf.h>/a #include <linux\/susfs.h>' ./fs/proc/base.c
              fi
              if [[ "$CURRENT_SUB" -le 75 && "${{ inputs.os_patch_level }}" != "2024-05" ]]; then
                sed -i -e 's/goto show_pad;/return 0;/' ./fs/proc/task_mmu.c
              fi
            fi

            # Android 15 - 6.6 S+«sñì
            if [[ "${{ inputs.android_version }}" == "android15" && "${{ inputs.kernel_version }}" == "6.6" ]]; then
              if [[ "$CURRENT_SUB" -ge 98 ]]; then
                cp $KERNEL_PATCHES/wild/susfs_fix_patches/v${SUSFS_VERSION}/a15-6.6/base.c.patch ./
                patch -p1 < base.c.patch || true
              fi
              # S+«sñì 6.6.50~6.6.97: SUSFS FíÑS+üs+òtö¿S¦å AS_FLAGS_SUS_MAP / susfs_is_current_proc_umounted_app
              # S+åt+¦s¦æ #include <linux/susfs_def.h>n+îs»+Fç¦t+ûF»æµèÑ undeclared identifier
              if grep -q 'AS_FLAGS_SUS_MAP\|susfs_is_current_proc_umounted' ./fs/proc/base.c && ! grep -qF 'susfs_def.h' ./fs/proc/base.c; then
                if ! grep -qF '#include <linux/dma-buf.h>' ./fs/proc/base.c; then
                  sed -i '/#include <linux\/cpufreq_times.h>/a #include <linux\/dma-buf.h>' ./fs/proc/base.c
                fi
                sed -i '/#include <linux\/dma-buf.h>/a #endif' ./fs/proc/base.c
                sed -i '/#include <linux\/dma-buf.h>/a #include <linux\/susfs_def.h>' ./fs/proc/base.c
                sed -i '/#include <linux\/dma-buf.h>/a #ifdef CONFIG_KSU_SUSFS_SUS_MAP' ./fs/proc/base.c
              fi
              # S+«sñì ReSukiSU: SUSFS FíÑS+üS++tö¿ SUSFS_IS_INODE_SUS_MAP / SUSFS_IS_INODE_OPEN_REDIRECT
              # S+åt+¦s¦æ #include <linux/susfs.h>n+îs»+Fç¦t+ûF»æµèÑ undeclared identifier
              if grep -q 'SUSFS_IS_INODE_SUS_MAP\|SUSFS_IS_INODE_OPEN_REDIRECT' ./fs/proc/base.c && ! grep -qF '#include <linux/susfs.h>' ./fs/proc/base.c; then
                if ! grep -qF '#include <linux/dma-buf.h>' ./fs/proc/base.c; then
                  sed -i '/#include <linux\/cpufreq_times.h>/a #include <linux\/dma-buf.h>' ./fs/proc/base.c
                fi
                sed -i '/#include <linux\/dma-buf.h>/a #include <linux\/susfs.h>' ./fs/proc/base.c
              fi
              if [[ "$CURRENT_SUB" -le 58 ]]; then
                cp $KERNEL_PATCHES/wild/susfs_fix_patches/v${SUSFS_VERSION}/a15-6.6/task_mmu.c.patch ./
                patch -p1 < task_mmu.c.patch || true
              fi
            fi

            # Android 16 - 6.12 S+«sñì
            if [[ "${{ inputs.android_version }}" == "android16" && "${{ inputs.kernel_version }}" == "6.12" ]]; then
              # S+«sñì setuid_hook.c Tçìsñìs«ÜS¦ë: s+ô CONFIG_KSU_SUSFS sÉ»tö¿µù¦n+î
              # s¦òTâ¿ #if (>=6.8 && MANUAL_HOOK) s¥ùS+ÜS¦ºtöƒt¼¼S¦îS+¬ ksu_handle_setresuid s«ÜS¦ë
              SETUID_HOOK="$KERNEL_ROOT/common/drivers/kernelsu/setuid_hook.c"
              if [ -f "$SETUID_HOOK" ]; then
                sed -i 's/defined(CONFIG_KSU_MANUAL_HOOK))/!defined(CONFIG_KSU_SUSFS) \&\& defined(CONFIG_KSU_MANUAL_HOOK))/' "$SETUID_HOOK"
                echo "s+¦S+«sñì setuid_hook.c Tçìsñìs«ÜS¦ëTù«Tóÿ"
              fi
            fi
          fi

      # ==================== Unicode t+òF+çS+«sñì ====================
      # S+«sñì Unicode T¢¦s«+s¡ùt¼ªt+òF+çµúÇµ¦ïtÜäTù«Tóÿ
      - name: s¦ötö¿ Unicode t+òF+çS+«sñì
        if: inputs.enable_susfs
        working-directory: ${{ env.KERNEL_ROOT }}/common
        run: |
          if [ "${{ inputs.kernel_version }}" = "5.10" ] || [ "${{ inputs.kernel_version }}" = "5.15" ]; then
            patch -p1 --forward < "$ACTION_BUILD/patches/unicode_bypass_fix_6.1-.patch" || true
          else
            patch -p1 --forward < "$ACTION_BUILD/patches/unicode_bypass_fix_6.1+.patch" || true
          fi

      # ==================== sìçt¦º LZ4 s¦¦s¦ötö¿ LZ4KD/LZ4K_OPLUS FíÑS+ü ====================
      # t+ƒS+ÇsñätÉå ZRAM t¢+sà¦tÜä LZ4 sÅÿµ¢¦n+îsçÅs¦æTçìsñì step
      - name: Tàìt+« ZRAM LZ4 FíÑS+üµáê
        if: inputs.use_zram
        working-directory: ${{ env.KERNEL_ROOT }}/common
        run: |
          # t¼¼S+ÇTÿ¦µ«¦n+Üsìçt¦º LZ4 sê¦ 1.10.0 s¦¦µ++sèá ARM64 NEON sèáTÇƒ
          echo "sìçt¦º LZ4..."
          rm -f lib/lz4/lz4_compress.c lib/lz4/lz4_decompress.c lib/lz4/lz4defs.h lib/lz4/lz4hc_compress.c

          cp -r $ZZH_PATCHES/zram/lz4/* ./lib/lz4/
          cp -r $ZZH_PATCHES/zram/include/linux/* ./include/linux/
          bash $ZZH_PATCHES/zram/apply_lz4_neon.sh

          if [ -f "fs/f2fs/Makefile" ] && ! grep -qF "f2fs-\$(CONFIG_F2FS_IOSTAT) += iostat.o" "fs/f2fs/Makefile"; then
            echo "f2fs-\$(CONFIG_F2FS_IOSTAT) += iostat.o" >> "fs/f2fs/Makefile"
          fi

          # t¼¼S¦îTÿ¦µ«¦n+Üs¦ötö¿ LZ4KD sÆî LZ4K_OPLUS FíÑS+ü
          cp -r $SUKISU_PATCHES/other/zram/lz4k/include/linux/* ./include/linux/
          cp -r $SUKISU_PATCHES/other/zram/lz4k/lib/* ./lib/
          cp -r $SUKISU_PATCHES/other/zram/lz4k/crypto/* ./crypto/
          cp -r $SUKISU_PATCHES/other/zram/lz4k_oplus ./lib/

          cp $SUKISU_PATCHES/other/zram/zram_patch/${{ inputs.kernel_version }}/lz4kd.patch ./
          if ! patch -p1 -F 3 < lz4kd.patch; then
            echo "::warning::lz4kd.patch s¦ötö¿sñ¦F¦Ñn+îsÅ»Fâ+s+¦s¦ötö¿µêûS+èS+ïµûçS+ìsî¦Tàì"
          fi

          cp $SUKISU_PATCHES/other/zram/zram_patch/${{ inputs.kernel_version }}/lz4k_oplus.patch ./
          if ! patch -p1 -F 3 < lz4k_oplus.patch; then
            echo "::warning::lz4k_oplus.patch s¦ötö¿sñ¦F¦Ñn+îsÅ»Fâ+s+¦s¦ötö¿µêûS+èS+ïµûçS+ìsî¦Tàì"
          fi

      # ==================== S+«sñì 6.6 WiFi/Fô¥tëÖsà+s«¦µÇºn+êS+ëµÿƒ + s¦Åt¦¦n+ë ====================
      # sÉês¦¦S+ëµÿƒS+Äs¦Åt¦¦S+«sñìn+îs¦¦Tççtö¿s¦ét¡ësåÖµ¦òTü+sàìTçìsñìF++sèás»+Fç¦FäÅsÅÿµ¢¦
      - name: S+«sñì 6.6 WiFi/Fô¥tëÖsà+s«¦µÇºn+êS+ëµÿƒ + s¦Åt¦¦n+ë
        if: inputs.kernel_version == '6.6'
        working-directory: ${{ env.KERNEL_ROOT }}/common
        run: |
          # S+às£¿FíîS+ìs¡ÿs£¿µù¦F++sèán+îTü+sàìTçìsñìµëºFíîµ¦íµƒôµûçS+¦
          ensure_line_once() {
            local file="$1"
            local line="$2"
            if [ ! -f "$file" ]; then
              echo "::error::µûçS+¦S+ìs¡ÿs£¿: $file"
              exit 1
            fi
            if ! grep -qF "$line" "$file"; then
              echo "$line" >> "$file"
            fi
          }

          GALAXY_SYMBOL_LIST="android/abi_gki_aarch64_galaxy"
          XIAOMI_SYMBOL_LIST="android/abi_gki_aarch64_xiaomi"
          DRIVERS_MAKEFILE="drivers/Makefile"
          MIN_KDP_SRC="$KERNEL_PATCHES/samsung/min_kdp/min_kdp.c"
          MIN_KDP_PATCH="$KERNEL_PATCHES/samsung/min_kdp/add-min_kdp-symbols.patch"
          MIN_KDP_DST="drivers/min_kdp.c"

          # S+ëµÿƒn+ÜFíÑsàà KDP t¼ªsÅ+n+ês¦ét¡ën+ë
          ensure_line_once "$GALAXY_SYMBOL_LIST" "kdp_set_cred_non_rcu"
          ensure_line_once "$GALAXY_SYMBOL_LIST" "kdp_usecount_dec_and_test"
          ensure_line_once "$GALAXY_SYMBOL_LIST" "kdp_usecount_inc"

          # S+ëµÿƒn+ÜS+às£¿sÅ»s¦ötö¿µù¦µëôFíÑS+ün+îs+¦s¦ötö¿/S+ìsî¦TàìsêÖF+¦F+ç
          if [ ! -f "$MIN_KDP_PATCH" ]; then
            echo "::error::FíÑS+üS+ìs¡ÿs£¿: $MIN_KDP_PATCH"
            exit 1
          fi
          if patch -p1 --dry-run < "$MIN_KDP_PATCH" >/dev/null 2>&1; then
            patch -p1 --no-backup-if-mismatch < "$MIN_KDP_PATCH"
          else
            echo "min_kdp symbols patch s+¦s¦ötö¿µêûs+ôsëìS+èS+ïµûçS+ìsî¦Tàìn+îF+¦F+çpÇé"
          fi

          # S+ëµÿƒn+Üsñìsê¦T¬¦sè¿µ¦Étáüs¦¦tí«S+¥ Makefile S+àF++sèáS+Çµ¼í
          if [ ! -f "$MIN_KDP_SRC" ]; then
            echo "::error::µûçS+¦S+ìs¡ÿs£¿: $MIN_KDP_SRC"
            exit 1
          fi
          cp "$MIN_KDP_SRC" "$MIN_KDP_DST"
          ensure_line_once "$DRIVERS_MAKEFILE" "obj-y += min_kdp.o"

          # s¦Åt¦¦n+ÜFíÑsààt¼ªsÅ+n+ês¦ét¡ën+ë
          ensure_line_once "$XIAOMI_SYMBOL_LIST" "device_find_any_child"

      # ==================== sñçS++sÄƒsºï defconfig ====================
      # tö¿S¦ÄsÉÄt+¡töƒµêÉ defconfig fragmentn+êbazel trim sà+s«¦n+ë
      - name: sñçS++sÄƒsºï defconfig
        run: cp "$DEFCONFIG" "$DEFCONFIG.orig"

      # ==================== Tàìt+« ZRAM TÇëTí¦ ====================
      # sÉ»tö¿sÉätºì ZRAM sÄït+¬t«ùµ¦ò
      - name: Tàìt+« ZRAM TÇëTí¦
        if: inputs.use_zram
        run: |
          CONFIG_FILE="$DEFCONFIG"

          # 5.10 T£ÇFªüTó¥sñûsƒ¦tíÇTàìt+«
          if [ "${{ inputs.kernel_version }}" = "5.10" ]; then
            cat >> "$CONFIG_FILE" <<'EOF'
          CONFIG_ZSMALLOC=y
          CONFIG_ZRAM=y
          CONFIG_MODULE_SIG=n
          CONFIG_CRYPTO_LZO=y
          CONFIG_ZRAM_DEF_COMP_LZ4KD=y
          EOF
          fi

          # 5.15 / 6.1n+Üs¦åµ¿ís¥ùµö¦S+¦såàs+¦
          if [ "${{ inputs.kernel_version }}" != "6.6" ] && [ "${{ inputs.kernel_version }}" != "5.10" ]; then
            if grep -q "CONFIG_ZSMALLOC" "$CONFIG_FILE"; then
              sed -i 's/CONFIG_ZSMALLOC=m/CONFIG_ZSMALLOC=y/g' "$CONFIG_FILE"
            else
              echo "CONFIG_ZSMALLOC=y" >> "$CONFIG_FILE"
            fi
            sed -i 's/CONFIG_ZRAM=m/CONFIG_ZRAM=y/g' "$CONFIG_FILE"
          fi

          # 6.6n+Üs¦åµ¿ís¥ùµö¦S+¦såàs+¦
          if [ "${{ inputs.kernel_version }}" = "6.6" ]; then
            echo "CONFIG_ZSMALLOC=y" >> "$CONFIG_FILE"
            sed -i 's/CONFIG_ZRAM=m/CONFIG_ZRAM=y/g' "$CONFIG_FILE"
          fi

          # android14/15n+ÜS+Ä modules.bzl tº+TÖñ zram/zsmalloc µ¿ís¥ùsú¦µÿÄ
          if [ "${{ inputs.android_version }}" = "android14" ] || [ "${{ inputs.android_version }}" = "android15" ]; then
            sed -i 's/"drivers\/block\/zram\/zram\.ko",//g; s/"mm\/zsmalloc\.ko",//g' "$KERNEL_ROOT/common/modules.bzl"
          fi

          # sÉ»tö¿sÄït+¬t«ùµ¦òn+êS++tö¿tï¼t½ï fragment µûçS+¦n+ë
          if grep -q "CONFIG_ZSMALLOC=y" "$CONFIG_FILE" && grep -q "CONFIG_ZRAM=y" "$CONFIG_FILE"; then
            cat "$ZZH_PATCHES/config/zram.config" >> "$CONFIG_FILE"
          fi

      # ==================== µ++sèá BBG Tÿ¦µá+µ£¦FíÑS+ü ====================
      # BBG (Baseband Guard) Tÿ¦µ¡ósƒ¦s+ªµá+s+Åsîûµö+sç+
      - name: µ++sèá BBG Tÿ¦µá+µ£¦FíÑS+ü
        if: inputs.use_bbg
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          wget -O- https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash
          echo "CONFIG_BBG=y" >> common/arch/arm64/configs/gki_defconfig
          sed -i '/^config LSM$/,/^help$/{ /^[[:space:]]*default/ { /baseband_guard/! s/selinux/selinux,baseband_guard/ } }' common/security/Kconfig

      # ==================== Tàìt+«såàµá+TÇëTí¦ ====================
      # µ++sèá KernelSU sÆîsà¦S+ûs+àFªütÜäsåàµá+Tàìt+«
      - name: Tàìt+«såàµá+TÇëTí¦
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          cat >> "$DEFCONFIG" << 'EOF'
          CONFIG_KSU=y
          CONFIG_TMPFS_XATTR=y
          CONFIG_TMPFS_POSIX_ACL=y
          EOF

          # tº+TÖñ defconfig µúÇµƒÑ
          sed -i 's/check_defconfig//' ./common/build.config.gki

      - name: sÉ»tö¿kpm
        if: inputs.use_kpm
        run: |
          if [ "${{ inputs.ksu_variant }}" == "SukiSU" ] || [ "${{ inputs.ksu_variant }}" == "ReSukiSU" ] || [ "${{ inputs.ksu_variant }}" == "Next" ]; then
            echo "CONFIG_KPM=y" >> "$DEFCONFIG"
          fi

      # ==================== µ++sèá SUSFS Tàìt+« ====================
      # sÉ»tö¿ SUSFS sÉäTí¦TÜÉFùÅsèƒFâ+
      - name: µ++sèá SUSFS Tàìt+«
        if: inputs.enable_susfs
        run: |
          cat >> "$DEFCONFIG" << 'EOF'
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
          CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_MAP=y
          EOF

      # ==================== Tàìt+«såàµá+sÉìtº¦ ====================
      - name: Tàìt+«såàµá+sÉìtº¦
        working-directory: ${{ env.KERNEL_ROOT }}
        run: |
          if [ -f "build/build.sh" ]; then
            sed -i 's/-dirty//' ./common/scripts/setlocalversion
          else
            # Tàìt+« 6.1+ såàµá+
            sed -i '/^[[:space:]]*"protected_exports_list"[[:space:]]*:[[:space:]]*"android\/abi_gki_protected_exports_aarch64",$/d' ./common/BUILD.bazel
            sed -i '/kmi_symbol_list_strict_mode/d' ./common/BUILD.bazel
            rm -rf ./common/android/abi_gki_protected_exports_*
            sed -i "/stable_scmversion_cmd/s/-maybe-dirty//g" ./build/kernel/kleaf/impl/stamp.bzl
          fi

          # Tàìt+«såàµá+sÉìtº¦
          if [ -n "${{ inputs.version }}" ]; then
            CLEAN_VERSION=$(echo "${{ inputs.version }}" | sed -E 's/^[0-9]+\.[0-9]+\.[0-9]+//')
            
            # s«ÜS¦ë KMI_TAGn+êS+ÄFç¬sè¿tëêµ£¼S+¥µîüS+ÇFç¦n+ë
            case "${{ inputs.android_version }}-${{ inputs.kernel_version }}" in
              "android14-6.1")  KMI_TAG="android14-11" ;;
              "android15-6.6")  KMI_TAG="android15-8" ;;
              "android16-6.12") KMI_TAG="android16-5" ;;
              *) KMI_TAG="${{ inputs.android_version }}" ;;
            esac
            
            if [ "${{ inputs.kernel_version }}" = "6.1" ]; then
              # 6.1n+Üµùºµá+s+Ån+îsîàsÉ½ KMI_TAG
              KMI_LOCAL="-${KMI_TAG}${CLEAN_VERSION}"
              sed -i "\$s|echo \"\$res\"|echo \"${KMI_LOCAL}\"|" ./common/scripts/setlocalversion 2>/dev/null || true
              sed -i "/^CONFIG_LOCALVERSION=/ s/=\"[^\"]*\"/=\"${KMI_LOCAL}\"/" ./common/arch/arm64/configs/gki_defconfig 2>/dev/null || true
            elif [ "${{ inputs.kernel_version }}" = "5.10" ] || [ "${{ inputs.kernel_version }}" = "5.15" ]; then
              # 5.xn+Üµùºµá+s+Ån+îS+ìsîàsÉ½ KMI_TAG
              sed -i "\$s|echo \"\$res\"|echo \"${CLEAN_VERSION}\"|" ./common/scripts/setlocalversion 2>/dev/null || true
              sed -i '/^CONFIG_LOCALVERSION=/ s/="\([^"]*\)"/="'"$CLEAN_VERSION"'"/' ./common/arch/arm64/configs/gki_defconfig
            else
              # 6.6+n+Üµû¦µá+s+Å setlocalversionn+îsîàsÉ½ KMI_TAG
              SUFFIX="-${KMI_TAG}${CLEAN_VERSION}"
              perl -i -0777 -pe 's/(.*)echo "\$\{KERNELVERSION\}\$\{file_localversion\}\$\{config_localversion\}\$\{LOCALVERSION\}\$\{scm_version\}"/$1echo "\$\{KERNELVERSION\}'"${SUFFIX}"'"/s' ./common/scripts/setlocalversion 2>/dev/null || true
            fi
          elif [ ! -f "build/build.sh" ]; then
            cd ./common
            BID="ab$((RANDOM % 90000000 + 10000000))"
            GHASH=$(git rev-parse --verify HEAD | cut -c1-13)
            # t+ƒS+Çsåàµá+sÉìtº¦µá+s+Ån+Ü-androidXX-KMI-gHASH-BID-4k
            case "${{ inputs.android_version }}-${{ inputs.kernel_version }}" in
              "android14-6.1")  KMI_TAG="android14-11" ;;
              "android15-6.6")  KMI_TAG="android15-8" ;;
              "android16-6.12") KMI_TAG="android16-5" ;;
              *) KMI_TAG="${{ inputs.android_version }}" ;;
            esac

            if [ "${{ inputs.kernel_version }}" = "6.1" ]; then
              # 6.1n+ÜTÇÜF+ç setlocalversion µ¦¿sàÑtëêµ£¼sÉìtº¦n+êµùºµá+s+Ån+ë
              KMI_LOCAL="-${KMI_TAG}-g${GHASH}-${BID}-4k"
              sed -i "\$s|echo \"\$res\"|echo \"${KMI_LOCAL}\"|" ./scripts/setlocalversion 2>/dev/null || true
              sed -i "/^CONFIG_LOCALVERSION=/ s/=\"[^\"]*\"/=\"${KMI_LOCAL}\"/" ./arch/arm64/configs/gki_defconfig 2>/dev/null || true
            else
              # 6.6+n+ÜS+«µö¦ setlocalversion µ¦¿sàÑtëêµ£¼sÉìtº¦n+êµû¦µá+s+Ån+îsÅéFÇâ wild_kerneln+ë
              # S+¥tòÖ ${KERNELVERSION} sÆî ${config_localversion} tö¦FäÜµ£¼F+ÉFíîµù¦FºúµPÉ
              SUFFIX="-${KMI_TAG}-g${GHASH}-${BID}"
              perl -i -0777 -pe 's/(.*)echo "\$\{KERNELVERSION\}\$\{file_localversion\}\$\{config_localversion\}\$\{LOCALVERSION\}\$\{scm_version\}"/$1echo "\$\{KERNELVERSION\}'"${SUFFIX}"'\$\{config_localversion\}"/s' ./scripts/setlocalversion 2>/dev/null || true
            fi
          fi

      # ==================== Fç¬s«ÜS¦ëµPäs+¦µù¦Tù¦ ====================
      - name: F«+t+«Fç¬s«ÜS¦ëµPäs+¦µù¦Tù¦
        shell: bash
        run: |
          # F»+sÅûF+ôsàÑn+ÜF+ôsàÑ N/n µêûtòÖt¬¦µù¦n+îS++tö¿s+ôsëì UTC µù¦Tù¦
          INPUT_TIME="${{ inputs.build_time }}"
          if [[ -n "$INPUT_TIME" && "$INPUT_TIME" != "N" && "$INPUT_TIME" != "n" ]]; then
            DATESTR="$INPUT_TIME"
            echo "S++tö¿F+ôsàÑtÜäµPäs+¦µù¦Tù¦: $DATESTR"
          else
            DATESTR="$(TZ='UTC' date +'%a %b %d %T %Z %Y')"
            echo "S++tö¿s+ôsëì UTC µù¦Tù¦: $DATESTR"
          fi

          # s»+sç¦t+ÖsÉÄt+¡t+ûF»æµ¦üt¿ï
          echo "KBUILD_BUILD_TIMESTAMP=$DATESTR" >> "$GITHUB_ENV"
          echo "KBUILD_BUILD_VERSION=1" >> "$GITHUB_ENV"

          # s¢¦s«ÜsÅ¬sñätÉå common/scripts/mkcompile_h
          f="$KERNEL_ROOT/common/scripts/mkcompile_h"
          if [ -f "$f" ]; then
            echo "såÖsàÑ mkcompile_h µPäs+¦µù¦Tù¦: $DATESTR"
            if grep -q 'UTS_VERSION=' "$f"; then
              perl -pi -e "s{UTS_VERSION=\"\\\$\\(.*?\\)\"}{UTS_VERSION=\"#1 SMP PREEMPT $DATESTR\"}" "$f"
            else
              perl -0777 -pi -e "s{cat <<EOF}{cat <<EOF\n#undef UTS_VERSION\n#define UTS_VERSION \"#1 SMP PREEMPT $DATESTR\" } unless /UTS_VERSION/" "$f"
            fi
          else
            echo "µ£¬µë+sê¦t¢«µáçµûçS+¦n+îF+¦F+ç: $f"
          fi

      # ==================== t+ûF»æsåàµá+ ====================
      # S++tö¿TçìF»òµ£¦sê¦t+ûF»æsåàµá+n+îF¦àµù¦sÉÄFç¬sè¿TçìF»ò
      - name: µPäs+¦GÇöGÇösªésç¦TöÖS+¦F»ÑKSUµÜéµù¦µ£¬sÉîµ¡ÑSUSFSS+útáüµêût+ûF»æµù¦TöÖF»»(t¡ës+àF»ÑKSUS+ôs¦ôS+«sñì)
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 30
          max_attempts: 3
          retry_on: timeout
          command: |
            echo "::group::µPäs+¦F»¦µÿÄ"
            echo "S+ÇFê¼µâàså¦S+ïS+ìS+Üsç¦TöÖn+îTÖñT¥PKSUS+ôs¦ôsÆîSUSFSS+ôs¦ôS+útáüS+ìsÉîpÇé"
            echo "S+ìFªüTù«n+îS+ìFªüµÇÑn+îF+ÖF+ƒS+áµÇÄS¦êsè¬sè¢n+îµêûFÇàF+ƒµêæsÅìTªêµ¦ísà¦t¦+n+îs¢áS+¦F+ÖF+ƒµêæµùásà¦pÇé"
            echo "t¡ës+àTÇéTàìsì¦sÅ»n+îS+áµ£ëFâ+sè¢sêÖFç¬s+¦sÄ+SUKISUS+ôs¦ôµÅÉS¦ñS+útáün+î"
            echo "::endgroup::"
            
            echo "s+ôsëì KSU µ£Çµû¦µÅÉS¦ñµùÑµ£ƒ: ${KSU_LATEST_COMMIT_DATE}"
            echo "s+ôsëì SUSFS µ£Çµû¦µÅÉS¦ñµùÑµ£ƒ: ${SUSFS_LATEST_COMMIT_DATE}"
            set -ex
            cd "$KERNEL_ROOT"

            sed -i 's/BUILD_SYSTEM_DLKM=1/BUILD_SYSTEM_DLKM=0/' ./common/build.config.gki.aarch64
            sed -i '/MODULES_ORDER=android\/gki_aarch64_modules/d' ./common/build.config.gki.aarch64
            sed -i '/KMI_SYMBOL_LIST_STRICT_MODE/d' ./common/build.config.gki.aarch64

            if [ -f "build/build.sh" ]; then
              # 5.x såàµá+S++tö¿S+át+ƒµPäs+¦FäÜµ£¼
              LTO=thin BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh CC="/usr/bin/ccache clang" || exit 1
            else
              # 6.1+ såàµá+S++tö¿ Bazel µPäs+¦
              # µÅÉsÅû gki_defconfig S+«µö¦sê¦ fragmentn+îTü+sàì bazel trim µúÇµƒÑsñ¦F¦Ñ
              FRAG="common/arch/arm64/configs/ksu.fragment"
              diff "$DEFCONFIG.orig" "$DEFCONFIG" | grep '^>' | sed 's/^> //; s/^[[:space:]]*//' > "$FRAG" || true
              cp "$DEFCONFIG.orig" "$DEFCONFIG"
              echo "=== KSU Fragment såàs«¦ ==="
              cat "$FRAG"
              echo "========================="
              # S+às+ô fragment T¥Pt¬¦µù¦S+áTÇÆsÅéµò¦
              FRAG_FLAG=""
              if [ -s "$FRAG" ]; then
                FRAG_FLAG="--defconfig_fragment=//common:arch/arm64/configs/ksu.fragment"
              fi
              # 6.12+ S++tö¿ lto=nonen+êRust såàµá+µ¿ís¥ùS+ìsà+s«¦ thin LTOn+ë
              LTO_FLAG="--lto=thin"
              if [ "${{ inputs.kernel_version }}" = "6.12" ]; then
                LTO_FLAG="--lto=none"
              fi
              tools/bazel build --disk_cache=/home/runner/.cache/bazel --config=fast $LTO_FLAG $FRAG_FLAG //common:kernel_aarch64_dist || exit 1
              strings ./bazel-bin/common/kernel_aarch64/Image | grep 'Linux version'
            fi

            echo "S+ÇFê¼µâàså¦S+ïS+ìS+Üsç¦TöÖn+îTÖñT¥PKSUS+ôs¦ôsÆîSUSFSS+ôs¦ôS+útáüS+ìsÉîpÇé"
            echo "S+ìFªüTù«n+îS+ìFªüµÇÑn+îF+ÖF+ƒS+áµÇÄS¦êsè¬sè¢n+îµêûFÇàF+ƒµêæsÅìTªêµ¦ísà¦t¦+n+îs¢áS+¦F+ÖF+ƒµêæµùásà¦pÇé"
            echo "t¡ës+àTÇéTàìsì¦sÅ»n+îS+áµ£ëFâ+sè¢sêÖFç¬s+¦sÄ+SUKISUS+ôs¦ôµÅÉS¦ñS+útáün+î"
            echo "s+ôsëì KSU µ£Çµû¦µÅÉS¦ñµùÑµ£ƒ: ${KSU_LATEST_COMMIT_DATE}"
            echo "s+ôsëì SUSFS µ£Çµû¦µÅÉS¦ñµùÑµ£ƒ: ${SUSFS_LATEST_COMMIT_DATE}"
            echo "sªéµP£µùÑµ£ƒS+ìsÉîµêût¢+s+«F+çF+£sêÖFíÑS+üsñ¦µòêpÇüt+ûF»æsñ¦F¦Ñ"
      # ==================== KPM FíÑS+ü (ReSukiSU) ====================
      # S+¦ ReSukiSU s¦ötö¿ KPM (Kernel Patch Module) FíÑS+ü
      - name: s¦ötö¿ KPM FíÑS+ü (ReSukiSU)
        if: inputs.use_kpm
        run: |
          case "${{ inputs.android_version }}" in
            "android12"|"android13")
              if [ "${{ inputs.kernel_version }}" != "6.6" ]; then
                TARGET_PATH="$KERNEL_ROOT/out/${{ inputs.android_version }}-${{ inputs.kernel_version }}/dist"
              fi
              ;;
            "android14"|"android15"|"android16")
              TARGET_PATH="$KERNEL_ROOT/bazel-bin/common/kernel_aarch64/"
              ;;
          esac

          cp -r $SUKISU_PATCHES/kpm/patch_linux "$TARGET_PATH/patch"
          cd "$TARGET_PATH"
          chmod 777 patch
          ./patch
          rm -rf Image
          mv oImage Image

      # ==================== sçåsñç Boot Tò£sâÅ ====================
      # sñìsê¦t+ûF»æS¦ºtë¬sê¦s+ÑS+£t¢«s+ò
      - name: sçåsñç Boot Tò£sâÅ
        run: |
          mkdir -p bootimgs

          if [ "${{ inputs.android_version }}" == "android12" ] || [ "${{ inputs.android_version }}" == "android13" ]; then
            SRC_DIR="$KERNEL_ROOT/out/${{ inputs.android_version }}-${{ inputs.kernel_version }}/dist"
          else
            SRC_DIR="$KERNEL_ROOT/bazel-bin/common/kernel_aarch64"
          fi

          cp "$SRC_DIR/Image" ./bootimgs/
          cp "$SRC_DIR/Image.lz4" ./bootimgs/
          cp "$SRC_DIR/Image" ./
          cp "$SRC_DIR/Image.lz4" ./
          gzip -n -k -f -9 ./Image > ./Image.gz

      # ==================== sê¢s+¦ AnyKernel3 sÄït+¬sîà ====================
      - name: sê¢s+¦ AnyKernel3 sÄït+¬sîà
        run: |
          cd $ANYKERNEL3
          ZIP_NAME="${{ inputs.android_version }}-${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.os_patch_level }}-AnyKernel3.zip"
          mv ../Image ./Image
          zip -r "../$ZIP_NAME" ./*

      # ==================== µPäs+¦ Boot Tò£sâÅ (Android 12) ====================
      # Android 12 T£ÇFªüS+Ä GKI F«ñF»üTò£sâÅS+¡µÅÉsÅû ramdisk
      - name: µPäs+¦ Boot Tò£sâÅ (Android 12)
        if: inputs.android_version == 'android12'
        working-directory: bootimgs
        run: |
          GKI_URL=https://dl.google.com/android/gki/gki-certified-boot-android12-5.10-${{ inputs.os_patch_level }}_${{ inputs.revision }}.zip
          FALLBACK_URL=https://dl.google.com/android/gki/gki-certified-boot-android12-5.10-2023-01_r1.zip

          status=$(curl -sL -w "%{http_code}" "$GKI_URL" -o /dev/null)
          if [ "$status" = "200" ]; then
            curl -Lo gki-kernel.zip "$GKI_URL"
          else
            curl -Lo gki-kernel.zip "$FALLBACK_URL"
          fi

          unzip gki-kernel.zip && rm gki-kernel.zip
          $UNPACK_BOOTIMG --boot_img="$(pwd)/boot-5.10.img"

          gzip -n -k -f -9 ./Image > ./Image.gz

          # sê¢s+¦µÖ«TÇÜ boot.img
          $MKBOOTIMG --header_version 4 --kernel Image --output boot.img --ramdisk out/ramdisk --os_version 12.0.0 --os_patch_level "${{ inputs.os_patch_level }}"
          $AVBTOOL add_hash_footer --partition_name boot --partition_size $((64 * 1024 * 1024)) --image boot.img --algorithm SHA256_RSA2048 --key $BOOT_SIGN_KEY_PATH
          cp ./boot.img ../${{ inputs.android_version }}-${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.os_patch_level }}-boot.img

          # sê¢s+¦ gzip sÄït+¬tÜä boot.img
          $MKBOOTIMG --header_version 4 --kernel Image.gz --output boot-gz.img --ramdisk out/ramdisk --os_version 12.0.0 --os_patch_level "${{ inputs.os_patch_level }}"
          $AVBTOOL add_hash_footer --partition_name boot --partition_size $((64 * 1024 * 1024)) --image boot-gz.img --algorithm SHA256_RSA2048 --key $BOOT_SIGN_KEY_PATH
          cp ./boot-gz.img ../${{ inputs.android_version }}-${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.os_patch_level }}-boot-gz.img

          # sê¢s+¦ lz4 sÄït+¬tÜä boot.img
          $MKBOOTIMG --header_version 4 --kernel Image.lz4 --output boot-lz4.img --ramdisk out/ramdisk --os_version 12.0.0 --os_patch_level "${{ inputs.os_patch_level }}"
          $AVBTOOL add_hash_footer --partition_name boot --partition_size $((64 * 1024 * 1024)) --image boot-lz4.img --algorithm SHA256_RSA2048 --key $BOOT_SIGN_KEY_PATH
          cp ./boot-lz4.img ../${{ inputs.android_version }}-${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.os_patch_level }}-boot-lz4.img

      # ==================== µPäs+¦ Boot Tò£sâÅ (Android 13+) ====================
      # Android 13 sÅèS+ÑS+ètëêµ£¼S+ìT£ÇFªü ramdisk
      - name: µPäs+¦ Boot Tò£sâÅ (Android 13+)
        if: inputs.android_version == 'android13' || inputs.android_version == 'android14' || inputs.android_version == 'android15' || inputs.android_version == 'android16'
        working-directory: bootimgs
        run: |
          gzip -n -k -f -9 ./Image > ./Image.gz

          # sê¢s+¦µÖ«TÇÜ boot.img
          $MKBOOTIMG --header_version 4 --kernel Image --output boot.img
          $AVBTOOL add_hash_footer --partition_name boot --partition_size $((64 * 1024 * 1024)) --image boot.img --algorithm SHA256_RSA2048 --key $BOOT_SIGN_KEY_PATH
          cp ./boot.img ../${{ inputs.android_version }}-${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.os_patch_level }}-boot.img

          # sê¢s+¦ gzip sÄït+¬tÜä boot.img
          $MKBOOTIMG --header_version 4 --kernel Image.gz --output boot-gz.img
          $AVBTOOL add_hash_footer --partition_name boot --partition_size $((64 * 1024 * 1024)) --image boot-gz.img --algorithm SHA256_RSA2048 --key $BOOT_SIGN_KEY_PATH
          cp ./boot-gz.img ../${{ inputs.android_version }}-${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.os_patch_level }}-boot-gz.img

          # sê¢s+¦ lz4 sÄït+¬tÜä boot.img
          $MKBOOTIMG --header_version 4 --kernel Image.lz4 --output boot-lz4.img
          $AVBTOOL add_hash_footer --partition_name boot --partition_size $((64 * 1024 * 1024)) --image boot-lz4.img --algorithm SHA256_RSA2048 --key $BOOT_SIGN_KEY_PATH
          cp ./boot-lz4.img ../${{ inputs.android_version }}-${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.os_patch_level }}-boot-lz4.img

      # ==================== µö¦T¢åFíÑS+üså¦t¬üµûçS+¦ ====================
      # µö¦T¢åµëÇµ£ë .rej µûçS+¦tö¿S¦ÄF¦âF»òFíÑS+üsñ¦F¦ÑTù«Tóÿ
      - name: µö¦T¢åFíÑS+üså¦t¬üµûçS+¦
        if: always()
        run: |
          REJECTS_DIR="$GITHUB_WORKSPACE/patch-rejects"
          mkdir -p "$REJECTS_DIR"

          mapfile -t REJS < <(find "$KERNEL_ROOT" -type f -name '*.rej')
          REJ_COUNT=${#REJS[@]}
          echo "sÅætÄ¦ $REJ_COUNT S+¬ .rej µûçS+¦"
          echo "REJ_COUNT=$REJ_COUNT" >> $GITHUB_ENV

          if [ "$REJ_COUNT" -gt 0 ]; then
            for REJ in "${REJS[@]}"; do
              REL="${REJ#"$KERNEL_ROOT"/}"
              DEST="$REJECTS_DIR/$REL"
              mkdir -p "$(dirname "$DEST")"
              cp "$REJ" "$DEST"

              ORIG="${REJ%.rej}"
              if [ -f "$ORIG" ]; then
                cp "$ORIG" "${DEST%.rej}"
              fi
              echo "$REL" >> "$REJECTS_DIR/index.txt"
            done
          fi

      # ==================== S+èS+áµPäs+¦S¦ºtë¬ ====================
      - name: S+èS+áµPäs+¦S¦ºtë¬
        uses: actions/upload-artifact@v4
        with:
          name: ReSukiSU_kernel-${{ env.CONFIG }}
          path: |
            *AnyKernel3.zip
            *.img
          compression-level: 9

      # ==================== S+èS+áFíÑS+üså¦t¬üµûçS+¦ ====================
      - name: S+èS+áFíÑS+üså¦t¬üµûçS+¦
        if: always() && env.REJ_COUNT > 0
        uses: actions/upload-artifact@v4
        with:
          name: ReSukiSU_kernel-${{ env.CONFIG }}-Rejects
          path: patch-rejects/
          if-no-files-found: ignore
          compression-level: 9
